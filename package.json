{
  "name": "esp32-mic-relay",
  "version": "1.0.0",
  "description": "WebSocket relay that pipes live mic audio from an ESP32 to a browser, with no port forwarding on either side.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
